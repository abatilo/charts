apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "debug-playground.fullname" . }}-test-connection"
  labels:
    {{- include "debug-playground.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "debug-playground.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
