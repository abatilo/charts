apiVersion: v1
kind: Service
metadata:
  name: {{ include "debug-playground.fullname" . }}
  labels:
    {{- include "debug-playground.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "debug-playground.selectorLabels" . | nindent 4 }}
